mod bus;
mod constants;
mod cpu;
mod datas;
mod interfaces;
mod ram;

use crate::datas::snakes::SNAKE_MEMORY_MAPS;

fn main() {
    //let rom = Rom::new(*SNAKE_MEMORY_MAPS);
    //let mut cpu = CPU::new(rom);
    //cpu.reset();
    //cpu.run();
    ////cpu.display_rom();
    println!("End Program..");
}
